import React from 'react';
import './LoadingSpinner.css'; // Make sure to create this CSS file in the same folder

const LoadingSpinner = () => {
    return (
        <div className="loading-spinner"></div>
    );
}

export default LoadingSpinner;
